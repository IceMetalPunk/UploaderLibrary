/*********************************************************************/
/*                  UPLOADER LIBRARY - README                        */
/*                                                                   */
/* The Uploader Library was created by Daniel Burnett (IceMetalPunk) */
/* and is free to use and modify under the GPLv3 license, with the   */
/* stipulation that attribution be given to me upon release of copied*/
/* or derivative code.                                               */
/*                                                                   */
/*********************************************************************/

Javascript Classes:
  Uploader
    -Uploader(picker[, bar])
    -Upload(chunksize[, overwrite?][, name])
    -abort()
    -getPercent()
    -setReceiver(url)
    -setDir(dir)
    -onprogress
    -onload

  ProgressBar
    -ProgressBar(parent)
    -setSize(width, height)
    -setColors(FGcolor, BGcolor)
    -setAmount(amount, max)
    -setPercent(percent)
    -getPercent()
